#!/usr/bin/python3


def search_replace(my_list, search, replace):
    n = []
    for x in my_list:
        if e == search:
            n.append(replace)
        else:
            n.append(e)
    return n
